# ============================================
# Smart Pantry - Environment Variables
# ============================================

# ============================================
# Docker Port-Konfiguration
# ============================================
# WICHTIG: Container-Ports sind hardcoded (Frontend: 3000, Backend: 3001)
# Nur FRONTEND_PORT ist konfigurierbar für den Host-Port des Frontends
FRONTEND_PORT=3000

# ============================================
# Datenbank-Konfiguration
# ============================================
POSTGRES_DB=smart_pantry
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_postgres_password_here

# ============================================
# Backend-Konfiguration
# ============================================
# WICHTIG: BACKEND_PORT wird in Docker auf 3001 hardcoded gesetzt
# Diese Variable wird nur für lokale Entwicklung benötigt
BACKEND_PORT=3001

# Security-Keys (MUSS gesetzt werden!)
# Generiere PERSONAL_DATA_KEY mit: openssl rand -hex 32
PERSONAL_DATA_KEY=your_personal_data_key_here_generate_with_openssl_rand_hex_32
JWT_SECRET=your_jwt_secret_here_generate_secure_random_string

# Token & Quota Configuration (MUSS gesetzt werden!)
# Refresh Token TTL in Millisekunden (30 Tage = 2592000000)
REFRESH_TOKEN_TTL_MS=2592000000
# Quota Reset Interval in Millisekunden (30 Tage = 2592000000)
QUOTA_RESET_INTERVAL_MS=2592000000

# LLM Token Costs (MUSS gesetzt werden!)
LLM_TOKEN_COST_CHAT=your_real_cost_data
LLM_TOKEN_COST_TRANSLATION=your_real_cost_data
LLM_TOKEN_COST_ANALYZE=your_real_cost_data

# Recipe Call Costs (MUSS gesetzt werden!)
RECIPE_CALL_COST_ANALYZE=your_real_cost_data

# ============================================
# API Keys (Optional)
# ============================================
# Gemini API Key für Foto-Analyse und Chat
# Ohne Key werden Stub-Daten verwendet
GEMINI_API_KEY=

# Spoonacular API Key für Rezeptvorschläge
# Ohne Key werden Stub-Daten verwendet
SPOONACULAR_API_KEY=

# GitHub Token für Issue-Erstellung (Optional)
GITHUB_TOKEN=

# ============================================
# Admin Account Configuration (Optional)
# ============================================
# Der Admin-Account wird automatisch beim Server-Start erstellt
# Falls nicht gesetzt, werden Standard-Werte verwendet
# Ändere diese Werte für Production!
ADMIN_USER_EMAIL=your_admin_email_here
ADMIN_USER_NAME=your_admin_name_here
ADMIN_USER_PASSWORD=your_secure_admin_password_here
ADMIN_USER_FULLNAME=your_admin_fullname_here
ADMIN_USER_USERNAME=your_admin_username_here

# ============================================
# Frontend-Konfiguration
# ============================================
# Mock-Auth für lokale Entwicklung ohne Datenbank
# In Produktion immer auf false setzen!
NEXT_PUBLIC_USE_MOCK_AUTH=false

# Öffentliche API-URL (Optional)
# Nur setzen, wenn das Backend unter einer extern erreichbaren Domain liegt
# Standardmäßig nutzt das Frontend den /api Proxy
# NEXT_PUBLIC_API_URL=https://api.smartpantry.example

# ============================================
# WICHTIGE HINWEISE
# ============================================
# - NEXT_INTERNAL_API_URL ist in Docker hardcoded auf http://smart-pantry-backend:3001
# - PORT und HOSTNAME werden in Docker hardcoded gesetzt (nicht konfigurierbar)
# - Backend ist nur intern im Docker-Netzwerk erreichbar
# - Frontend kommuniziert ausschließlich über Docker-Netzwerk mit Backend
# - Nur FRONTEND_PORT ist konfigurierbar für den Host-Port
